# Dependencias para la aplicacion web (app/main.py)
# Instalar con: pip install -r requirements-app.txt

# Interfaz web
streamlit>=1.28.0

# Generacion de PDFs
fpdf2>=2.8.7

# Manipulacion de datos (tablas en la app)
pandas>=2.0.0

# Tablas formateadas en la CLI
tabulate>=0.9.0
